# Stop if a function requires disabled behaviors

Stop if a function requires disabled behaviors

## Usage

``` r
require_cache_behaviors(behaviors)
```

## Arguments

- behaviors:

  a character vector of behavior flags to assert as requirements for
  metadata caching. values must have an entry found in
  riskmetric:::cache_behaviors list

## Value

a boolean value
