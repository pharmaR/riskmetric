# Retrieve output of covr::package_coverage

Retrieve output of covr::package_coverage

## Usage

``` r
# S3 method for class 'covr_coverage.pkg_source'
pkg_ref_cache(x, ...)
```

## Value

a `pkg_ref` object
